name: pre_release

on: workflow_dispatch

jobs:
  build:

    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Get the version
      id: get_version
      uses: mavrosxristoforos/get-xml-info@1.0
      with:
        xml-file: '.\Directory.Build.props'
        xpath: '//Version'
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -c Release .\RoadCaptain.sln
    - name: Test
      run: dotnet test --no-build --verbosity normal -c Release
    - name: Publish Runner
      run: dotnet publish --no-restore --no-build -c Release src\RoadCaptain.Runner\RoadCaptain.Runner.csproj
    - name: Publish RouteBuilder
      run: dotnet publish --no-restore --no-build -c Release src\RoadCaptain.RouteBuilder\RoadCaptain.RouteBuilder.csproj
    - name: Update Wix components file
      run: dotnet run --project .\packaging\RoadCaptain.WixComponentFileGenerator\RoadCaptain.WixComponentFileGenerator.csproj src\RoadCaptain.Runner\bin\Release\net6.0-windows\publish src\RoadCaptain.RouteBuilder\bin\Release\net6.0-windows\publish .\packaging\RoadCaptain.Installer\Components.wxi
    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v1.1
    - name: Build MSI
      run: msbuild .\packaging\RoadCaptain.Installer\RoadCaptain.Installer.wixproj -property:Configuration=Release
    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.get_version.outputs.info }}
        release_name: ${{ steps.get_version.outputs.info }}
        draft: true
        prerelease: true
    - name: Upload Installer
      id: upload-release-asset 
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
        asset_path: D:\a\RoadCaptain\RoadCaptain\packaging\RoadCaptain.Installer\bin\Release\RoadCaptain_${{ steps.get_version.outputs.info }}.msi
        asset_name: RoadCaptain_${{ steps.get_version.outputs.info }}.msi
        asset_content_type: application/x-msi