name: build-and-test

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths-ignore:
      - site/**
  pull_request:
    branches: [ main ]
    paths-ignore:
      - site/**

jobs:
  build_win:

    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: dotnet test --verbosity normal -c Release

  build_macos-x64:

    runs-on: macos-11

    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: dotnet test --verbosity normal -c Release